{% set name = "antspyx" %}
{% set version = "0.6.3" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/antspyx-{{ version }}.tar.gz
  sha256: 48d1f5b6b2e8a90f6f66f14cdbdd4c37e20268f5db84cb9ac99256ecb9c72386
  patches:
    - itk_patch.diff

build:
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0
  skip: true  # [win]

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - {{ stdlib("c") }}
    - make  # [unix]
    - ninja  # [win]
    - scikit-build
    - cmake >=3.18
    - ants  # [not win]
  host:
    - python
    - scikit-build-core >=0.4.3
    - pip
    - libpng
    - libitk-devel
    - nanobind >=1.3.2
  run:
    - python
    - pandas
    - pyyaml
    - numpy <2.4.0
    - statsmodels
    - webcolors
    - matplotlib-base
    - pillow
    - requests
    - scikit-learn
    - scipy <1.16

test:
  imports:
    - ants
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/ANTsX/ANTsPy
  summary: A fast medical imaging analysis library in Python with algorithms for registration, segmentation, and more.
  license: Apache-2.0
  license_file: LICENSE

extra:
  recipe-maintainers:
    - sarthakpati
